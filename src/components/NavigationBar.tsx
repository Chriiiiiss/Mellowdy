import { Container, Text } from '@radix-ui/themes';

export const NavigationBar = () => {
  return (
    <Container size="4" style={{ backgroundColor: 'maroon' }}>
      <Text align={'center'} color="crimson">
        Je suis la NavBar
      </Text>
    </Container>
  );
};
